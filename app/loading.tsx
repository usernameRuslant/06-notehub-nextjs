'use client';

// interface LoadingProps {
//   loading: Loading;
// }

const Loading = () =>
  // { loading }: LoadingProps
  {
    return (
      <div>
        <p>Loading, please wait...</p>
      </div>
    );
  };

export default Loading;
